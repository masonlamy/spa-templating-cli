import React from 'react';
import styled from 'styled-components';

const Styled{name} = styled.div`

`;

const {name} = (props) =>
    <Styled{name}>
        Template
    </Styled{name}>;

{name}.propTypes = {

};

export default {name};